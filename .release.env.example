# Copy this file to .release.env and fill the Open VSX token locally.
# .release.env is ignored by git.
OVSX_PAT=

# Marketplace uses Microsoft Entra ID through Azure CLI (az login).
# No Marketplace token is stored here; legacy VSCE_PAT values are ignored.
# Optional publishing tenant when the account belongs to multiple directories.
AZURE_TENANT_ID=
